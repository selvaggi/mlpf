Install needed packages in a virtual environment with
```
python3 -m venv myenv
source myenv/bin/activate
pip install numpy plotly uproot
```

If you are in a fresh shell, and the virtual environment is already setup, activate it with
```
source myenv/bin/activate
```

Then:
```
python draw_events.py
```
